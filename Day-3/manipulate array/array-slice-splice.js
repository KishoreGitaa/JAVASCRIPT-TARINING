// array slice
// slice  is the copy the items 
const num = [1,2,3,4,5]
console.log(num.slice(0,1));
console.log(num);

// array splice
// splice is the remove the items

const call = ["milk","tea","coffer","boost"]
console.log(call.splice(0,2));

const numbers = ["milk","tea","coffer","boost"]
numbers.splice(0,2)
console.log(numbers)   

