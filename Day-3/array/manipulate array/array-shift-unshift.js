// unshift add the array in beginning
const addnum = [1,2,3,4,5]
addnum.unshift(0)
console.log("add the array in beginning using unshift",addnum);

// shift remove array in beginning 
addnum.shift()
console.log("remove the array in beginning using shift",addnum);