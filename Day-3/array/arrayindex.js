// Array can have items of different data types
const arr2 = [
    'Kishore',
    250,
    true,
    { country: 'india', city: 'tamilnadu' },
    { skills: ['HTML', 'CSS', 'JS', 'React', 'Python'] }
] // arr containing different data types
console.log(arr2)

// array using  using index

let companies = ["google", "microsoft", "tesla", "wipro"]
const indiaCompamies = companies;
console.log(indiaCompamies);
console.log(indiaCompamies.length);
console.log(indiaCompamies[2]);
let lastindex = indiaCompamies.length -1 
console.log(indiaCompamies[lastindex]);
